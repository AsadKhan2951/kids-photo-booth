# Kids Photo Booth (Next.js)

## Run locally
1) Install Node.js (LTS)
2) In this folder:
```bash
npm install
npm run dev
```
Open: http://localhost:3000

## Camera notes (browser)
- Use **Chrome** (recommended)
- When prompted, click **Allow camera**
- If permission blocks:
  - Chrome → 🔒 icon → Site settings → Camera → Allow

## Printing (browser)
This demo uses `window.print()` in a new tab.
For kiosk printing (silent print), normally Electron/Desktop apps are used.
